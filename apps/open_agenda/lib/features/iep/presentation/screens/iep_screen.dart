import 'package:flutter/material.dart';

class IEPDashboardScreen extends StatelessWidget {
  const IEPDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('IEP Management')),
      body: const Center(
        child: Text('IEP tracking coming soon'),
      ),
    );
  }
}
