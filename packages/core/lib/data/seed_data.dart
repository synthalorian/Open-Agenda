/// Seed data has been removed for privacy.
/// The app now starts completely empty.
/// Teachers fill in their own students, grades, and data.
class SeedData {
  static Future<void> seedIfEmpty() async {
    // No-op: everything starts blank
  }
}
